@import "px2rem.styl"

$imageSize( path, type = 'px' )
  if type == 'px'
    width: image-size( path )[0]
    height: image-size( path )[1]
  else if type == 'rem'
    width: $px2rem( image-size( path )[0] )
    height: $px2rem( image-size( path )[1] )

